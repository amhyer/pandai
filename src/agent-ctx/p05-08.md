# Work Record: p05-08

## Task: Fix P0 Security Findings (P0-05 through P0-08)

### P0-05: GURU Soal View — Replace MOCK_SOAL
**File**: `src/components/views/guru-views.tsx`
- Removed `MOCK_SOAL` array definition (12 fake question entries)
- Replaced `GuruSoalView` to fetch from `/api/questions?schoolId={user.schoolId}&limit=50`
- Added `soalList` state, `loading` state, `fetchSoal` callback with `useEffect`
- Maps API Question fields to Soal type: `type` (pg→PG, isian→Isai, esai→Esai), `difficulty` (mudah→Mudah, etc.), `status` (published→Terpublikasi, draft→Draft), `hots` (cognitiveLevel C4-C6)
- Added loading spinner and empty state 'Belum ada soal' when API returns empty
- On error, sets `soalList` to empty array (no mock fallback)

### P0-06: GURU Tryout View — Replace MOCK_TRYOUT
**File**: `src/components/views/guru-views.tsx`
- Removed `MOCK_TRYOUT` array definition (6 fake tryout entries)
- Replaced `GuruTryoutView` to fetch from `/api/exams?type=session&schoolId={user.schoolId}`
- Added `tryoutList` state, `loading` state, `fetchTryout` callback with `useEffect`
- Maps ExamSession fields: `status` (active→Aktif, scheduled→Terjadwal, ended→Selesai), `duration`, `questionCount` from examPackage, `participants` from assignments count
- Added loading spinner and empty state 'Belum ada tryout' when API returns empty

### P0-07: ADMIN SCHOOL — Remove PLACEHOLDER/MOCK fallbacks
**File**: `src/components/views/admin-school-views.tsx`
- Removed all placeholder array definitions: `PLACEHOLDER_CLASSES`, `PLACEHOLDER_EXAM_PACKAGES`, `PLACEHOLDER_CLASS_OPTIONS`, `PLACEHOLDER_ASSIGNMENTS`, `PLACEHOLDER_ANALYSIS`, `PLACEHOLDER_REPORTS`
- `ClassesView.fetchData`: On error/no schoolId → `setClasses([])` with `toast.error('Gagal memuat data')`
- `ExamAssignmentsView.fetchData`: On error → `setAssignments([])` with toast error
- `ExamAssignmentsView`: Added `examPackages` and `classOptions` state, `fetchDropdowns` that fetches from `/api/exams` and `/api/classes` APIs. Dropdowns show 'Tidak ada paket/kelas' when empty.
- `AnalyticsView.fetchData`: Now sets `setAnalysis([])` (no fake data)
- `ReportsView.fetchData`: Now sets `setReports([])` (no fake data)
- **Note**: `MOCK_ACTIVITY_LOGS` and `MOCK_TEACHERS` were not found in admin-school-views.tsx (they use variable name `n` in admin-school-new-views.tsx)

### P0-08: Global 401 Session Expiry Handler
**Created**: `src/lib/api-client.ts`
- `apiClient()` wrapper function with same signature as `fetch`
- Checks `response.status === 401`, calls registered callback
- `setOnUnauthorized(callback)` / `clearUnauthorizedHandler()` to manage the callback

**Modified**: `src/app/authenticated-app.tsx`
- `useEffect` registers unauthorized handler: clears user (`setUser(null)`), navigates to login (`setCurrentView('login')`), shows toast 'Sesi Anda telah berakhir. Silakan masuk kembali.'
- Cleanup on unmount via `clearUnauthorizedHandler()`

**Wired apiClient in 6 dashboard components**:
1. `src/components/dashboard/guru/guru-dashboard.tsx` — 2 fetch calls
2. `src/components/dashboard/admin-sekolah/admin-sekolah-dashboard.tsx` — 2 fetch calls
3. `src/components/dashboard/super-admin/super-admin-dashboard.tsx` — 2 fetch calls
4. `src/components/dashboard/siswa/siswa-dashboard.tsx` — 1 fetch call
5. `src/components/dashboard/orang-tua/orang-tua-dashboard.tsx` — 1 fetch call
6. `src/components/dashboard/kepala-sekolah/kepala-sekolah-dashboard.tsx` — 1 fetch call

All lint checks pass. No other files modified.